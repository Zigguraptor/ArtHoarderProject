<Window x:Class="ArtHoarderClient.View.Windows.InputStringDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:viewModels="clr-namespace:ArtHoarderClient.ViewModels"
        Title="{Binding Title}"
        Width="320" Height="160">
    <Window.DataContext>
        <viewModels:InputStringDialogViewModel />
    </Window.DataContext>
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="200" Margin="10">
        <TextBlock Text="{Binding ToolTip}" Margin="0 0 0 5" />
        <TextBox Margin="0 0 0 10" Text="{Binding Input}" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Padding="6,2,6,2" Content="Cancel" IsCancel="True" Margin="0 0 10 0" Click="ButtonClose_OnClick" />
            <Button Padding="6,2,6,2" Content="OK" IsDefault="True" Click="ButtonOk_OnClick" />
        </StackPanel>
    </StackPanel>
</Window>