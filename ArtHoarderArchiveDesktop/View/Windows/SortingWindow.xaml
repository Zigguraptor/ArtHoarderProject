<Window x:Class="ArtHoarderClient.View.Windows.SortingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:ArtHoarderClient.View.Windows"
        xmlns:entity="clr-namespace:ArtHoarderCore.DAL.Entities;assembly=ArtHoarderCore"
        xmlns:viewModels="clr-namespace:ArtHoarderClient.ViewModels"

        Title="{Binding Title}" Height="300" Width="450">
    <Window.DataContext>
        <viewModels:SortingWindowViewModel />
    </Window.DataContext>
    <DockPanel LastChildFill="False" Margin="16">
        <StackPanel DockPanel.Dock="Top">
            <TextBlock Margin="0,0,0,0" Text="By similar" />
            <DockPanel>
                <TextBlock DockPanel.Dock="Left" Margin="16,0,8,0" Text="Algorithm:" />
                <ComboBox IsEditable="False" ItemsSource="{Binding AvailableAlgorithms}"
                          SelectedItem="{Binding SelectedAlgorithm}" />
            </DockPanel>
        </StackPanel>

        <DockPanel DockPanel.Dock="Bottom" LastChildFill="False">
            <Button DockPanel.Dock="Right" Padding="8,4,8,4" Content="Apply" 
                    Click="ButtonBase_OnClick"/>
        </DockPanel>
    </DockPanel>
</Window>